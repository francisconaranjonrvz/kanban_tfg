# nginx como proxy inverso de Django + ACME challenge para Let's Encrypt.

events {}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;
    server_tokens off;
    client_max_body_size 10M;

    upstream django_backend {
        server app:8000;
    }

    # ---------- HTTP (sin certificado aún) ----------
    server {
        listen 80;
        server_name francisconaranjonrvz.es;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            proxy_pass http://django_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto http;
        }
    }

    # ---------- HTTPS tras certbot ----------
    # server {
    #     listen 443 ssl;
    #     http2 on;
    #     server_name francisconaranjonrvz.es;
    #
    #     ssl_certificate     /etc/letsencrypt/live/francisconaranjonrvz.es/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/francisconaranjonrvz.es/privkey.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_prefer_server_ciphers on;
    #     ssl_session_cache shared:SSL:10m;
    #     ssl_session_timeout 1d;
    #
    #     location / {
    #         proxy_pass http://django_backend;
    #         proxy_set_header Host              $host;
    #         proxy_set_header X-Real-IP         $remote_addr;
    #         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto https;
    #         proxy_redirect off;
    #     }
    # }
}
